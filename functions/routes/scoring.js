const express = require('express');
const router = express.Router();

// Placeholder for scoring routes
router.get('/gameweeks', (req, res) => {
  res.json({ message: 'Scoring system to be implemented' });
});

module.exports = router;